apiVersion: v1
kind: Service
metadata:
  name: {{ template "dotnetmicroserviceskubernetes.fullname" . }}
  labels:
    app: {{ template "dotnetmicroserviceskubernetes.name" . }}
    chart: {{ template "dotnetmicroserviceskubernetes.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "dotnetmicroserviceskubernetes.name" . }}
    release: {{ .Release.Name }}
